import org.w3c.dom.Node;

public class ImplementDFSUsingStack {
    
    /*
    Given the root of a binary tree, traverse the tree in DFS(preorder) and print each nodes.
    */

    public void dfsStack(Node node) {
    if(node == null) {
      return;
    }

    

}